@model EzInvoice.Models.User
@{
    Layout = "_MainLayout";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/css/EditUserStyleSheet.css" />
    <title>Edit Account information</title>
</head>
<body>
    <h4 class="text-center">Update Personal Information</h4>
    <div class="form panel panel-success">
        <div class="panel-body">

            @using (Html.BeginForm("Update", "User", FormMethod.Post))
            {
                @Html.ValidationSummary(true)
            <fieldset>
                <div>
                    @Html.EditorFor(model => model.FirstName)
                    @Html.ValidationMessageFor(model => model.FirstName)
                </div>
                <div>
                    @Html.EditorFor(model => model.LastName)
                    @Html.ValidationMessageFor(model => model.LastName)
                </div>
                <div>
                    @Html.EditorFor(model => model.EmailAddress)
                    @Html.ValidationMessageFor(model => model.EmailAddress)
                </div>
                <p>
                    <input type="submit" value="Update" />
                </p>
            </fieldset>
            }

            @*         <form class="form-group" asp-action="Update" asp-controller="User">

            <div asp-validation-summary="All"></div>

            <div class="form-group">
                <label asp-for="FirstName">First Name: </label>
                <input placeholder="@Model.FirstName" class="form-control" asp-for="FirstName" />
            </div>

            <div class="form-group">
                <label asp-for="LastName">Last Name: </label>
                <input placeholder="@Model.LastName" class="form-control" asp-for="LastName" />
            </div>

            <div class="form-group">
                <label asp-for="EmailAddress">Email address: </label>
                <input placeholder="@Model.EmailAddress" class="form-control" asp-for="EmailAddress" />
            </div>


            <div class="text-center">
                <input class="btn btn-success" type="submit" name="submit" value="Update Information" />
            </div>


        </form>
            *@
        </div>
    </div>

</body>
</html>
